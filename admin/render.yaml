services:
  - type: web
    name: hanrao-prime-portal
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node render-server.mjs
    envVars:
      - key: NODE_VERSION
        value: 20
